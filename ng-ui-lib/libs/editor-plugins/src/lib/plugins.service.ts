import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class EditorPluginsService {
  registerPlugin(name: string, plugin: any): void {
    // Implementation placeholder
  }
}