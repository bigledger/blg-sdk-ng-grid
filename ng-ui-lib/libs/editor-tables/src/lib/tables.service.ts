import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class EditorTablesService {
  insertTable(rows: number, cols: number): void {
    // Implementation placeholder
  }
}